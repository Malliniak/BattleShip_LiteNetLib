namespace BattleShipShared.Packets.Rooms
{
    public enum RoomCommand
    {
        CREATE_ROOM,
        CREATE_ROOM_CONFIRMATION,
        JOIN_ROOM,
        JOIN_ROOM_CONFIRM,
        ROOM_UPDATE,
        LOAD_GAME_SCENE,
        LOAD_GAME_SCENE_CONFIRM
    }
}